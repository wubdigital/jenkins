# Dockerfile for Web Service
FROM node:20-alpine
WORKDIR /app

ARG BUILD_NUMBER=0
ARG COMMIT_SHA=unknown

ENV BUILD_ID=$BUILD_NUMBER
ENV COMMIT_HASH=$COMMIT_SHA

COPY package*.json ./
RUN npm install
COPY . .

EXPOSE 8080
CMD ["node", "app.js"]